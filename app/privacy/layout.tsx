import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Distrozi - Privacy Policy",
  description: "Distrozi Privacy Policy.",
};

export default function PrivacyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
