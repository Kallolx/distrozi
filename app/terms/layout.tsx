import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Distrozi - Terms of Service",
  description: "Distrozi Terms of Service.",
};

export default function TermsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
